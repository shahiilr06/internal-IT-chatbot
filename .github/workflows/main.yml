name: Nexira IT Assistant CI/CD

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  backend-ci:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Run Backend Tests
      run: |
        pytest tests/test_backend.py

  frontend-ci:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: "20"
        
    - name: Install Frontend dependencies
      run: |
        cd frontend
        npm install
        
    # Add frontend linting/testing if available
    #- name: Run Lint
    #  run: cd frontend && npm run lint

  docker-build-check:
    runs-on: ubuntu-latest
    needs: [backend-ci, frontend-ci]
    steps:
    - uses: actions/checkout@v3
    
    - name: Build Backend Docker Image
      run: docker build -f backend.Dockerfile -t nexira-backend:latest .
      
    - name: Build Frontend Docker Image
      run: docker build -f frontend.Dockerfile -t nexira-frontend:latest .

  # CONTINUOUS DEPLOYMENT (TEMPLATE)
  # deployment:
  #   runs-on: ubuntu-latest
  #   needs: docker-build-check
  #   if: github.ref == 'refs/heads/main'
  #   steps:
  #   - name: Deploy to Server
  #     run: |
  #       echo "Add your deployment scripts here (e.g., Push to Docker Hub, Deploy to AWS/Azure)"
